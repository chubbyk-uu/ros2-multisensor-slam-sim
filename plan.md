# 项目路线图

本页只维护当前目标、已完成阶段的摘要和收口范围。详细实施清单、历史决策和
逐项整改记录见[计划历史档案](docs/archive/project-plan-history.md)；当前状态见
[项目状态](docs/status.md)。

## 项目目标

在 ROS 2 Jazzy 与 Gazebo Sim 上构建可扩展的差速轮式机器人仿真平台，按
“模型与时间空间契约 → 传感器 → 成熟基线 → 自动回归 → 自研替换”的顺序，完成：

1. 2D LiDAR 建图、定位与导航；
2. 自研 2D 激光 SLAM；
3. 3D LiDAR 成熟基线与自研 3D SLAM；
4. 在线导航和 Frontier Exploration；
5. 完成 RTAB-Map RGB-D 建图/定位与 3D LiDAR Nav2 障碍感知的松耦合导航链路，
   并保留自研 3D LiDAR SLAM 作为独立算法链路。

## 已完成阶段

- 2D 官方链路：SLAM Toolbox、地图保存、AMCL、Nav2 与自动回归。
- 自研 2D SLAM：相关匹配、退化处理、关键帧、后台回环、Ceres 位姿图和地图重建。
- 轮速 + IMU：协方差适配、`robot_localization` 二维 EKF、正常/打滑回归。
- 3D 成熟基线：RTAB-Map、MOLA、二维导航投影和高度语义障碍验收。
- 自研 3D SLAM：GICP 局部前端、Scan Context + GICP 回环、后台 SE(2) 位姿图、
  全局点云、二维投影、快照恢复和只读 localization。
- Frontier Exploration：在线建图、Nav2 调度、自动快照、安全随机出生、三世界
  独立验证，以及 Nav2 依赖故障的可分类退出。
- RGB-D 导航：RTAB-Map 使用 RGB-D 和轮速 + IMU 里程计完成建图/定位，Nav2 使用
  深度投影地图规划，独立 3D LiDAR 为代价地图和碰撞监视器提供障碍数据。

## 当前工程约束

- 所有仿真节点使用 `use_sim_time`；完整 TF 与包职责以
  [系统架构](docs/architecture.md) 为准。
- `/ground_truth/odom` 只用于评分和测试控制，禁止进入 SLAM、EKF 或导航估计。
- 2D 与 3D LiDAR 模型互斥；同一时间只有一个节点发布 `map -> odom`。
- 大场景、固定包和活动 campaign 的结论必须区分：固定输入验证算法行为，
  活动仿真验证导航与物理交互。

## 项目收口

### 3D 可靠性与可比性收尾

1. 已完成：以固定输入完成低位回波、统计离群点及其组合的受控对照；三种过滤均被
   误差、退化或实时预算拒绝，默认滤波保持不变。结果见
   [性能与标定](docs/performance.md#固定包地面与离群点-ab)。
2. 已完成：同一固定 3D 输入上的轨迹漂移、地图一致性、回环和资源对照。资源复测中
   自研聚合预处理与匹配器两个进程，两侧各三趟完整；连同首批轨迹后，开回环六趟的
   观测差距为 `2.25x`–`4.43x`，关闭回环首批三趟为 `13.42x`–`17.47x`。结果和边界见
   [性能与标定](docs/performance.md#完整进程资源复测与六趟轨迹观察)。
3. 已完成：2D 与自研 3D 链路均通过动态障碍和完全封路验收；3D 两种场景分别执行
   正向运行与可执行负向对照。公共判据、门限来源见
   [验证方法](docs/methodology.md#封路与动态障碍的判据)，结果见
   [验收记录](docs/acceptance.md#动态障碍与完全封路)。仍保留一条边界——超出机器人单次视野的
   封路，当前配置下 Nav2 无法判定为不可达，见
   [工程事件](docs/incidents.md#代价地图会忘记看不见的封路)。

### RGB-D 最终路线

1. 已完成：添加可选 RGB-D 相机，锁定 `640 × 480 @ 30 Hz`、`0.20–6.00 m`
   数据契约，完成内参、外参、时间戳、图像桥接、可选点云与 RViz 验收；稠密点云
   因实测负载默认关闭，大消息 writer 独立使用 Fast DDS SHM 容量保护且不阻塞
   `/clock`、里程计、IMU 与控制消息。
2. 已完成：成熟 RTAB-Map RGB-D 在线链路、固定输入契约、带纹理两圈包回归，以及
   完整 `30 Hz` 在线活动闭环验收；相机长程实测 `30.304 Hz`，视觉回环、误差、
   资源和地图硬判据均通过。
3. 已完成最终组合入口：RTAB-Map 只消费 RGB-D 与轮速 + IMU `/odom`，负责建图、
   定位和 `map -> odom`；Nav2 使用其深度二维投影作全局规划，独立 3D LiDAR 负责
   局部/全局障碍层与碰撞监视。最终组合动态障碍回归中，RGB-D 渐进建图与正式
   导航均成功，3D LiDAR 障碍时延、净空、绕行、终点误差和碰撞判据全部通过。
4. 该组合是项目最终的松耦合职责分工，不是 LiDAR—视觉融合。自研 3D LiDAR SLAM、
   RTAB-Map 纯 LiDAR 和 RTAB-Map RGB-D 是可独立验证的不同链路，互不注入约束。

### 当前明确不做

- 不实现视觉回环候选辅助、视觉几何约束或 LiDAR—视觉联合位姿图。
- 不把 RGB-D 深度作为 3D LiDAR 障碍层的补充来源；导航安全链以 3D LiDAR 为准。
- 不增加双目相机，不融合 2D 与 3D LiDAR，也不把缺少逐点时间的仿真链路称为 LIO。
- 后续工作限于缺陷修复、依赖兼容、性能维护和已有自动回归，不再新增传感器主线。

## 开发与验证策略

每次只引入一个可验证功能：先检查节点、话题、时间戳、TF 与 RViz 空间对齐，再调
算法参数；正式结论必须记录输入、命令、门限、结果与已知边界。详细的 A/B、固定包、
活动 campaign 与故障注入方法见[验证方法](docs/methodology.md)。
