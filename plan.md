# ROS 2 Gazebo 多传感器 SLAM 仿真项目计划

## 1. 项目目标

在 ROS 2 Jazzy 和 Gazebo Sim 中搭建一个可扩展的差速轮式机器人仿真平台，并按以下顺序实现 SLAM：

1. 2D 激光 SLAM。
2. 2D 地图定位与自主导航。
3. 自研 2D 激光 SLAM 核心模块。
4. 3D 激光 SLAM。
5. 视觉 SLAM。
6. 多传感器融合。

2D 激光 SLAM、定位导航和自研 2D SLAM 基线均已完成；RTAB-Map 在线 3D
建图与 Nav2 高度语义导航基线也已通过验收。自研 3D 激光 SLAM 已完成
“在线建图、Nav2 导航、Frontier Exploration 自主探索”的首版完整闭环。
当前剩余主线是固定输入下与 RTAB-Map 的系统对比、地面/离群点预处理和动态
封路恢复专项，完成工程收尾后进入视觉 SLAM 与激光—视觉融合。

## 2. 当前环境

已确认具备以下基础环境：

- ROS 2 Jazzy。
- Gazebo Sim（`gz`）。
- `ros_gz_sim`、`ros_gz_bridge`。
- `robot_state_publisher`、Xacro。
- `slam_toolbox`。
- Nav2。

已建立 description、gazebo、bringup、slam、slam_3d 和 navigation 六个 ROS 2 包；
机器人、仿真、2D LiDAR、SLAM Toolbox、地图保存、AMCL、Nav2 和自研
C++ 2D SLAM 链路已经接通，并具备长闭环、快速旋转、退化走廊、重复
结构、大场景和固定 rosbag 离线回归。

主要开发语言约定：

- SLAM 核心算法和正式 ROS 2 节点使用 C++17。
- Python 用于 launch、数据分析、测试工具和算法原型。
- Xacro 管理机器人模型，YAML 管理可调参数。

## 3. 当前目录结构

```text
slam/
├── AGENTS.md
├── README.md
├── plan.md
├── docs/
├── maps/
├── src/
│   ├── slam_robot_description/
│   │   ├── urdf/
│   │   ├── rviz/
│   │   └── launch/
│   ├── slam_robot_gazebo/
│   │   ├── worlds/
│   │   ├── config/
│   │   ├── rviz/
│   │   └── launch/
│   ├── slam_robot_bringup/
│   │   ├── rviz/
│   │   └── launch/
│   ├── slam_robot_slam/
│   │   ├── config/
│   │   ├── launch/
│   │   ├── include/
│   │   ├── scripts/
│   │   ├── src/
│   │   └── test/
│   ├── slam_robot_slam_3d/
│   │   ├── config/
│   │   ├── launch/
│   │   ├── rviz/
│   │   ├── scripts/
│   │   └── test/
│   └── slam_robot_navigation/
│       ├── config/
│       ├── launch/
│       └── scripts/
├── build/
├── install/
└── log/
```

各包职责：

- `slam_robot_description`：URDF/Xacro、模型资源和模型显示。
- `slam_robot_gazebo`：世界文件、Gazebo 系统和 ROS-Gazebo 桥接。
- `slam_robot_bringup`：统一组织机器人、仿真和算法启动。
- `slam_robot_slam`：`slam_toolbox` 配置、自研 C++ 2D SLAM、数据集与回归工具。
- `slam_robot_slam_3d`：自研 3D SLAM、点云输入检查、成熟算法对照、快照与
  3D 回归工具。
- `slam_robot_navigation`：地图加载、AMCL、Nav2 配置、Frontier Exploration
  调度和导航回归。

## 4. 系统架构

### 4.1 数据流

```text
遥控节点 / Nav2
       |
       v
   /cmd_vel
       |
       v
Gazebo 差速驱动 ----> /odom
       |
       +------------> /joint_states

Gazebo 2D LiDAR ----> /scan

robot_state_publisher ----> /tf、/tf_static

/scan + TF + /odom ----> slam_toolbox ----> /map、map -> odom

/scan + /odom + TF ----> 自研相关匹配前端 ----> 激光里程计、关键帧、map -> odom
                                      |
                                      +----> 后台回环 + Ceres 位姿图
                                      +----> 优化位姿射线重放 ----> /custom_slam/map

/ground_truth/odom ----> 只供自动回归评估，不进入任一估计器
```

### 4.2 TF 结构

```text
map
└── odom
    └── base_footprint
        └── base_link
            ├── left_wheel_link
            ├── right_wheel_link
            ├── caster_link
            └── lidar_mount_link
                └── lidar_link
```

TF 发布职责：

- `map -> odom`：官方基线由 `slam_toolbox` 发布，自研入口由扫描匹配前端
  发布；两个入口不得同时运行。
- `odom -> base_footprint`：由差速驱动里程计发布。
- `base_footprint -> base_link` 及机器人结构：由 `robot_state_publisher` 发布。
- `base_footprint` 位于左右驱动轮轴线中点的地面投影，作为差速运动学旋转中心。
- 底盘几何中心相对驱动轴后移，质心投影保持在驱动轮与后万向轮形成的支撑三角形内。

## 5. 第一阶段：2D 激光 SLAM（已完成）

### 5.1 建立 ROS 2 包

任务：

- 创建 `src/`。
- 创建 description、gazebo、bringup 和 slam 四个基础包。
- 配置依赖、安装目录和基础 launch 文件。
- 建图链路稳定后再创建 navigation 包（已按此顺序完成）。

验收：

- `colcon build` 成功。
- 所有包可通过 `ros2 pkg list` 查到。
- `source install/setup.bash` 后可以启动基础 launch 文件。

### 5.2 创建差速轮式机器人模型

第一版机器人建议参数：

| 项目 | 初始值 |
| --- | --- |
| 底盘尺寸 | 0.45 × 0.32 × 0.12 m |
| 轮子半径 | 0.075 m |
| 轮宽 | 0.035 m |
| 几何轮距 | 0.34 m |
| Gazebo 有效运动学轮距 | 0.306 m（原地旋转标定值） |
| 雷达高度 | 0.25～0.35 m |

任务：

- 使用 Xacro 建立底盘、左右驱动轮和万向轮。
- 添加 visual、collision 和 inertia。
- 建立 `base_footprint`、`base_link` 和轮子关节。
- 添加 `lidar_link`，暂时只建立安装位置。
- 创建模型显示 launch 和 RViz 配置。

验收：

- Xacro 可以正确展开为 URDF。
- RViz 中模型尺寸、方向和关节正确。
- TF 树连通且没有重复发布。
- 无惯性矩阵和关节定义错误。

### 5.3 接入 Gazebo Sim

任务：

- 创建基础测试世界。
- 将机器人生成到 Gazebo。
- 配置关节状态发布。
- 配置差速驱动系统。
- 桥接 `/clock`、`/cmd_vel`、`/odom` 和 `/joint_states`。

验收：

- 机器人可以稳定生成，不弹飞、不下沉。
- 发布 `/cmd_vel` 后能前进、后退和转向。
- `/odom` 连续变化，运动方向与机器人坐标系一致。
- 直线运动无严重侧滑，旋转中心合理。

### 5.4 添加 2D 激光雷达

建议初始参数：

| 项目 | 初始值 |
| --- | --- |
| 扫描范围 | -180°～180° |
| 角分辨率 | 约 0.5° |
| 最小距离 | 0.12 m |
| 最大距离 | 12～20 m |
| 更新频率 | 10 Hz |

任务：

- 在 `lidar_link` 上添加 Gazebo GPU LiDAR 或 LiDAR 传感器。
- 将 Gazebo 激光数据桥接到 ROS 2 `/scan`。
- 设置正确的消息类型、QoS 和 `frame_id`。
- 在 RViz 中添加 LaserScan 显示。

验收：

- `/scan` 持续发布且频率正确。
- `frame_id` 与机器人 TF 一致。
- RViz 中激光点与墙体重合。
- 机器人运动时激光数据不会相对模型漂移。

### 5.5 创建 SLAM 测试世界

世界应包含：

- 封闭房间。
- 内部隔墙。
- 若干箱体或立柱。
- 一条可形成回环的通道。
- 足够的非对称几何特征。

验收：

- 场景可以稳定加载。
- 碰撞模型与视觉模型基本一致。
- 激光能观察到足够的几何特征。
- 仿真实时率能够满足 10 Hz 激光和 SLAM 运算。

### 5.6 接入 `slam_toolbox`

第一版使用 `online_async` 模式。

主要配置项：

- `use_sim_time`。
- `scan_topic`。
- `map_frame`、`odom_frame` 和 `base_frame`。
- 地图分辨率，初始值为 `0.05 m/cell`。
- 激光最大有效距离。
- 扫描匹配阈值。
- 关键帧插入距离和角度。
- 回环检测参数。

验收：

- `/map` 正常发布。
- 地图随机器人运动逐步扩展。
- 墙体没有明显双线和重影。
- 机器人绕场景一圈后可以产生合理回环。
- SLAM 过程中没有 TF 超时或时间戳错误。

### 5.7 保存地图

任务：

- 保存 `map.yaml` 和地图图像。
- 保存必要的 `slam_toolbox` 序列化地图。
- 编写地图保存说明和启动命令。

验收：

- 地图文件可以重新加载。
- 地图分辨率、原点和占据阈值正确。
- 重启系统后地图不会损坏或发生比例变化。

## 6. 第二阶段：定位与导航（基线已完成）

本阶段以 ROS 2 Jazzy 安装的 Nav2 官方示例为基线，保留官方完整组件，只适配机器人相关参数：

- 使用 Nav2 Map Server 加载已保存地图。
- 使用 AMCL 完成二维定位。
- 配置全局代价地图和局部代价地图。
- 配置规划器、控制器和恢复行为。
- 在 RViz 中设置初始位姿并发送导航目标。
- 保留官方 MPPI、Navfn、Simple Smoother、Behavior Server、Velocity Smoother、Collision Monitor、Waypoint Follower、Route Server 和 Docking Server。

验收：

- [x] Map Server 正确加载保存的 239 × 200、0.05 m/cell 地图。
- [x] AMCL 从已知出生点自动初始化，`map -> odom` 正常发布。
- [x] 激光与静态地图正确对齐，`/scan` 稳定为 10 Hz。
- [x] Nav2 生命周期节点全部进入 `active`。
- [x] 机器人完成 `(0, 0) -> (1, 0)` 导航测试并返回 `SUCCEEDED`。
- [x] 自动完成 9 个目标点、约 20 m 路线和返回原点的导航回归测试。
- [x] 加入地图外临时障碍物，验证代价地图更新和动态绕障。
- [ ] 设计完全封路场景，单独验证停车等待和 Nav2 恢复行为。

完全封路恢复属于导航专项补充测试，保留为非阻塞任务；它不影响已经通过
的地图定位、多目标导航和动态障碍物重规划基线，也不作为进入 3D 阶段的
前置条件。

2026-07-30 自动回归结果：

- 9/9 个目标点成功，总耗时 `98.5 s`。
- 最终剩余路径 `0.10 m`，返回原点的 AMCL 位置误差 `0.107 m`。
- Nav2 恢复次数为 0，Collision Monitor 未触发。
- 测试使用覆盖实际底盘和驱动轮的八点凸多边形 footprint。

2026-07-30 动态障碍物回归结果：

- 导航开始后生成地图中不存在的 `0.60 m × 0.60 m` 箱体。
- 局部、全局代价地图均将箱体标记为致命障碍。
- 剩余路径由 `4.19 m` 重规划为约 `5.00 m`，并在 `18.5 s`
  内成功到达目标。
- 机器人到障碍物的最小距离为 `1.083 m`，相对原直线路径的最大绕行
  偏移为 `1.207 m`，终点 AMCL 位置误差为 `0.173 m`。
- Nav2 恢复次数和 Collision Monitor 触发次数均为 0，说明正常重规划
  已及时避障；恢复行为尚未通过封路场景触发。
- 测试结束后动态箱体成功自动删除。

## 7. 第三阶段：自研 2D 激光 SLAM（已完成基线）

本阶段先以 SLAM Toolbox 建立成熟方案基线，再通过仿真和 rosbag 开发并
验证以下模块。通用录制入口保留动态 `/tf`；固定回归数据集只记录算法
输入、真值和 `/tf_static`，避免旧 `map -> odom` 泄漏到待测结果。

### 7.1 激光预处理

- [x] 增加 Gazebo 无噪声真值 `/ground_truth/odom`。
- [x] 录制 `/scan`、`/odom`、真值、时钟和 TF 的 MCAP 数据集。
- [x] 提供不启动 Gazebo 的数据集回放、预处理和 RViz 入口。
- [x] 去除 NaN、Inf 和越界距离。
- [x] 距离裁剪与可选降采样。
- [x] 将极坐标激光点转换到 `lidar_link` 二维点集。
- [x] 发布 `/custom_slam/scan_points` 并在 RViz 验证。
- [x] 为过滤、投影和降采样添加 C++ 单元测试。

### 7.2 帧间扫描匹配

- [x] 使用里程计作为位姿初值。
- [x] 实现点到线 ICP 对照算法和合成数据单元测试。
- [x] 对照 slam_toolbox/Karto 与 Cartographer 的扫描匹配结构。
- [x] 确定逐帧 ICP 只作算法基线，不作为默认 SLAM 前端。
- [x] 增加固定仿真路线，对比匹配轨迹、轮式里程计和 Gazebo 真值。

### 7.3 局部地图与关键帧

- [x] 建立最近关键帧组成的局部相关栅格。
- [x] 使用粗到细相关搜索将当前扫描与局部子图匹配。
- [x] 按平移、旋转阈值插入关键帧。
- [x] 使用匹配分数和最少重合点拒绝低质量结果。
- [x] 限制局部关键帧数量，并优化栅格去重和候选评分开销。
- [x] 增加两圈长路线和快速旋转自动回归测试。
- [x] 增加带入口/出口锚点的单向退化长走廊自动回归。
- [x] 增加走廊掉头返回自动回归，验证退化状态切换、真实回环和地图重建。
- [x] 增加重复房间混淆回归，以关键帧真值距离验证假回环保护。
- [x] 增加 155 m 大场景长时间仿真回归，采集误差、延迟、CPU、RSS 和 Path 上限。
- [x] 将固定的大场景 rosbag 纳入 2× 可重复离线回放回归。

### 7.4 占据栅格地图

- [x] 使用射线模型更新空闲和占据区域。
- [x] 使用 Bresenham 算法遍历栅格。
- [x] 使用 log-odds 表示占据概率。
- [x] 发布 `/custom_slam/map` 标准 `nav_msgs/OccupancyGrid`。
- [x] 在 RViz 中显示自研占据栅格。
- [x] 发布 `map -> odom` 校正，并将地图、匹配轨迹和激光统一到 `map`。

### 7.5 回环与位姿图

- [x] 保存全部成功关键帧，并建立位姿图节点。
- [x] 在相邻关键帧之间建立扫描匹配顺序约束。
- [x] 使用 Ceres 实现二维位姿图求解、角度流形和固定首节点。
- [x] 为回环边增加 Huber 鲁棒核并通过合成回环单元测试。
- [x] 发布 `/custom_slam/pose_graph_path` 验证关键帧图。
- [x] 排除近期节点并按空间距离搜索历史回环候选。
- [x] 使用候选附近多关键帧子地图和独立高门限相关匹配验证回环。
- [x] 回环通过后自动添加鲁棒边、运行 Ceres 并同步当前定位与关键帧路径。
- [x] 保存关键帧射线观测，优化后分批重投影并原子替换全局地图。

2026-07-30 自研回环回归结果：

- 圆形路线建立 100 个以上关键帧，自动检测到关键帧 `2 -> 100` 回环。
- 回环相关分数为 `0.954`，354 个点通过重合检查。
- Ceres 代价由 `0.020000` 降为 `0.000154`，4 次迭代收敛。
- 闭环停止后自研位置与 Gazebo 真值的平面位置差约 `0.010 m`；真值只
  用于回归测量，未反馈给算法。
- 地图重建从 101 个优化关键帧启动，追上运行期间新增帧后用 103 帧
  在 `580.8 ms` 内完成；分批执行期间未阻塞激光/里程计时间同步。
- 地图重建版闭环停止后，自研位置与 Gazebo 真值的平面位置差约
  `0.003 m`。

## 8. 第四阶段：3D 激光 SLAM（进行中）

### 8.1 3D LiDAR 模型与数据链路

- [x] 在 Xacro 中增加互斥的 `lidar_3d_mount_link -> lidar_3d_link` 固定链，
  默认保留现有 2D LiDAR，避免破坏 2D 回归基线和额外堆叠遮挡。
- [x] 将线数、水平/垂直视场角、量程、更新频率、噪声和安装位姿参数化。
- [x] 在 Gazebo Sim 中加入 3D GPU LiDAR，并通过 `ros_gz_bridge` 发布
  `/lidar_3d/points`（`sensor_msgs/PointCloud2`）。
- [x] 增加独立的 3D 传感器入口和 RViz 配置；默认 2D 启动方式保持
  兼容，不额外承担 3D 点云开销。
- [x] 检查点云 `frame_id`、仿真时间戳、频率、字段、点数、有限性、量程和
  TF 空间对齐。
- [x] 记录互斥 2D/3D 配置的实时率、Gazebo Server CPU/RSS、桥接 CPU/RSS
  和原始点云带宽，先满足稳定仿真再接算法。
- [ ] 补充 GPU 占用的受控测量；点云算法延迟已记录首版 MOLA 基线。

### 8.2 IMU 与 3D SLAM 成熟基线

- [x] 增加 `imu_link` 和 `/imu/data_raw`，参数化频率、噪声和偏置。
- [x] 静止时检查重力方向、角速度零偏、协方差、时间戳和外参。
- [x] 提供轮速 + IMU 二维 EKF，并在默认 2D 仿真、建图、导航和自研 SLAM
  入口启用，确保
  `/odom` 与 `odom -> base_footprint` 始终只有一个发布者。
- [x] 建立正常附着和单侧打滑的直行/旋转自动回归，量化轮式里程计、
  融合里程计和自研 2D SLAM 相对真值的差异。
- [x] 选择 ROS 2 Jazzy 官方二进制提供的 MOLA GICP 作为首条成熟基线；
  保留官方六自由度能力，先以纯 LiDAR odometry 模式适配本机器人。
- [x] 接入 ROS 2 Jazzy 二进制 RTAB-Map 作为在线完整基线：订阅局部
  `/odom` 与 `/lidar_3d/points`，启用 ICP 配准、空间邻近回环和位姿图，
  默认使用轮速 + IMU EKF 局部预测且仅发布 `map -> odom`；已完成点云
  契约、命名空间、地图话题和数据库烟雾验收。
- [x] 配置 RTAB-Map 3D 点云到二维占据栅格的投影：过滤地面、保留导航高度
  障碍物并启用二维射线填充，关键帧预生成局部栅格；增加地图尺寸、自由区和
  障碍区的输入契约检查。
- [x] 建立与旧 2D AMCL 链路互斥的 RTAB-Map + Nav2 在线导航启动入口：不启动
  Map Server 和 AMCL，`map -> odom` 仍由 RTAB-Map 独占；在官方
  `nav2_params.yaml` 之上只改写坐标系、足迹、`/rtabmap/map` 静态层和
  `PointCloud2` 观测源，并使局部体素层的垂直范围与导航高度带一致。
- [x] 在受控两圈闭环路线中验收 `/rtabmap/map` 的边界、障碍物和自由空间
  质量；增加 `structured_loop_regression.launch.py`，以硬判据检查路线、
  proximity 回环、`map -> odom` 修正、闭合误差、图节点和栅格质量。
- [x] 按 RTAB-Map 官方激光建图流程启用相邻关键帧 ICP 精修，以 EKF 为初值
  减少小型圆柱和墙角的局部重影；两圈回归与实时预算复验通过。
- [x] 建立 RTAB-Map + Nav2 高度语义多目标回归：两圈建图后实际穿越
  `0.55 m` 高门洞，并验证路径按 `0.40 m` 粗柱帽而不是 `0.07 m` 细底座
  绕行；地图语义、真值轨迹和动作结果均为硬判据。
- [x] 修复六自由度 ICP 使平面地图倾斜并把地面误标为实时障碍的问题：启用
  RTAB-Map 三自由度配准/里程计约束，并把 `map -> odom` 的 z 与倾角上限
  加入结构化回归硬判据。
- [ ] 补充 3D 在线导航的动态障碍物重规划和完全封路恢复，与既有 2D
  AMCL 导航回归对照。
- [x] 固化算法输入契约，检查点云字段、布局、点数、`frame_id` 和超时；
  明确关闭无逐点时间点云的 deskew，禁止 Gazebo 真值进入估计器。
- [x] 保持 TF 单一发布：MOLA 只发布 `map -> odom`，不发布自带的
  footprint TF；底盘里程计和 `robot_state_publisher` 继续负责其余边。
- [x] 完成首次直行加旋转验收，检查位姿质量、局部地图和 10 Hz 输出。
- [x] 建立包含立体结构和回环的 3D 世界：`structured_loop_3d.sdf`，`72 m`
  环形走廊，立体结构按 `±15°` 垂直视场沿视线布置而非靠加高墙体；已验证
  尺度、通行余量、两圈建图漂移收敛和资源占用。
- [x] 查清单圈回环不可靠的原因：**不是参数问题，是路线几何**。用固定包的
  真值轨迹统计"空间上近、图上远"的候选节点后，单圈全程 160 个节点里只有
  `3` 个（`1.9%`）存在重访候选（`3 m` 半径），两圈是 `169/169`（`100%`）；
  即使放宽到 `RGBD/LocalRadius` 的 `10 m`，单圈也只有 `11.2%`。`72 m` 闭环
  跑一圈只在最后几秒重访，命中与否取决于当时的累计漂移，这正是实测全有
  全无的原因。原假设"`RGBD/ProximityPathMaxNeighbors` 过小"对参数语义的
  理解也有误：官方默认 `0` 是**禁用** one-to-many 检测，`1` 已是启用。
  实测详见[性能说明](docs/performance.md)。
- [ ] 若需要单趟即可验收回环的快速回归，需新增中途折返的路线或世界
  （8 字形或带返回支路），现有 `structured_loop_3d` 只能靠两圈验收回环。
- [x] 给 3D 复合启动入口增加短时冒烟检查：五个入口均在无界面模式下等待
  关键节点连续在线 `60 s`，并检查启动日志；每个入口使用独立进程组后串行
  回收，数据集录制写入临时目录。该检查覆盖 `ffc720d` 曾触发的 deferred
  launch 参数作用域错误，不能用静态检查替代。
- [ ] 建立超过 40 m 直段的专用退化世界，验证 `Icp/PointToPlaneMinComplexity`
  和 `Icp/PointToPlaneLowComplexityStrategy` 这条已配置但从未被触发的
  低复杂度路径；现有环形世界的南段只是弱观测，不是秩亏。
- [x] 录制不含算法输出的固定 3D rosbag，固化话题契约、消息数量和文件哈希；
  自研算法离线判定器将在前端可运行后接入同一数据。

3D 阶段的“模型、点云、TF、性能、受控闭环建图、真实 proximity 回环和
Nav2 高度语义多目标导航”验收已经完成；在线算法基线确定为 RTAB-Map。
固定 3D 数据集、C++ 点云预处理、首版 PCL GICP 前端和有界局部子图已经
完成，专用长走廊的弱几何诊断与连续退化处理也已通过自动回归。自研回环
后端、全局地图、在线导航、版本化快照和自主探索现已完成并保留 RTAB-Map
同场景对照。下一主线收口固定输入性能/精度对比、地面与离群点预处理，以及
动态障碍物和完全封路恢复专项。

当前 Gazebo `PointCloud2` 只有 `x/y/z/intensity/ring`，没有厂家常见的
逐点时间字段。MOLA 基线因此是明确关闭去畸变的几何 LO，不伪造时间戳；
完整 LIO 将使用带真实逐点时间的厂家 rosbag 或 PCAP 验证。

### 8.3 自研 3D 激光 SLAM

实现顺序遵循成熟 3D 激光 SLAM 的模块边界，并始终保留 RTAB-Map 作为同场景
对照，不在第一版同时改动前端、后端和导航：

- [x] 录制不含算法输出和真值估计输入的固定 3D rosbag，固化点云、IMU、
  轮速、TF、频率和数据集指纹；仿真真值只供离线评分。
- [x] 建立自研 C++17 3D SLAM 包、参数、话题、TF 职责、诊断和最小单元测试；
  自研入口不得与 RTAB-Map/MOLA 同时发布同一段 TF。
- [x] 实现基础点云预处理：有限性与量程检查、机器人本体裁剪、PCL 体素
  降采样，并用固定包验证字段、点数和处理预算。
- [ ] 在前端确定地面约束表达后增加地面/非地面分类和可配置离群点过滤；
  不以删除全部地面作为默认策略。
- [x] 实现以轮速 + IMU 为运动初值的 PCL GICP scan-to-local-map 首版，输出
  收敛状态、RMSE、有效对应点、初值修正量和处理耗时，并用固定包校验轨迹。
- [x] 从最终对应点法向构造 `x/y/yaw` 点到平面信息矩阵和特征值退化诊断，
  并验证正常结构化路段零误报、合成平行走廊可检出。
- [x] 建立超过 40 m 的专用 3D 弱几何世界和自动回归；采用双阈值连续投影，
  严重退化时弱方向回退到轮速 + IMU 预测，强方向仍使用 GICP 修正，不以
  `hasConverged()` 代替几何可观测性。
- [x] 实现按平移/旋转阈值插入的关键帧和最近 12 帧有界局部子图，控制局部
  点云数量和内存。
- [x] 完成固定包直线路线，以及实时 Gazebo 三档快速旋转、专用弱几何
  长走廊和单侧打滑专项回归；冻结首版 matcher、关键帧与 12 帧局部子图
  参数，后续修改须复跑该集合。
- [x] 缓存子图版本未变化时的 GICP 目标点云与协方差；点云在前后两条里程计
  之间插值，并以有界 FIFO 等待尚未到达的后样本。
- [x] 将平移、平面和偏航退化分项诊断，按实际弱方向处理结果发布各向异性
  协方差；连续配准失败达到门限后清空并用当前扫描重播种局部子图。
- [x] 建立长期全局关键帧库：保存注册/占据点云、时间戳、前端 `x/y/yaw` 位姿、
  轮速 + IMU 预测和退化/协方差信息；局部 12 帧子图继续只服务实时前端，
  不能承担全局回环历史。快照共享不可变点云，可供后续后台回环线程读取；
  固定包前端回归检查关键帧数与累计点数。
- [x] 为全局关键帧实现 Scan Context 风格的 LiDAR 地点描述子与检索索引；候选
  排除时间相邻和累计行程过短的关键帧（不能按空间邻近排除真正的回访），并保留
  描述子分数、候选排名和预测偏航供诊断；候选仍须经后续几何复核才能接受。
- [x] 对前 N 个地点候选建立候选关键帧局部子图，使用 PCL GICP 做几何复核；
  同时通过收敛、对应点数、RMSE、重叠率、最大位姿修正和弱几何判据，不能仅凭
  描述子分数接受回环。检索与复核在后台顺序执行，通过后排队给位姿图任务，
  不会直接在前端回调校正轨迹。
- [x] 在后台工作线程对不可变关键帧快照构建平面 SE(2) 位姿图：顺序边使用
  前端相对位姿，回环边使用复核结果和鲁棒核；优化成功后事务式提交，发布唯一
  的标准 `map -> odom` 修正，不能阻塞 10 Hz 前端；修正严格投影为 SE(2)。
- [x] 增加自研 3D 回环正反端到端回归：固定包确认“候选检索 → GICP 复核 →
  后台图优化 → `map -> odom` 提交”，并要求至少一次提交、零丢弃/异常；弱几何
  走廊确认错误候选不会提交约束。2026-08-06 实时固定包结果为 2,999 帧、7 次
  提交、P95 `25.257 ms`，走廊结果为 0 次提交、P95 `46.349 ms`。
- [x] 优化后按全局关键帧重放重建 `/custom_slam_3d/map_cloud`：不可变扫描快照在
  有界后台批次中按优化位姿重放并增量体素化；2026-08-09 固定包验收持续发布
  38k 量级体素点，前端 P95 `25.876 ms`，7 次图提交均无失败。
- [x] 按机器人最高点及安全余量从优化关键帧射线持续生成 `/map`
  `OccupancyGrid`：`0.05–0.45 m` 高度带保留高门洞并阻挡上部外扩障碍物，未知
  空间不误标为自由；实现与单元测试已通过，供后续 Nav2 使用。
- [x] 将点云密度按职责拆分：预处理保留 `0.05 m` 建图点云，前端内部派生
  `0.10 m` 注册点云；GICP、局部子图、Scan Context、回环和三维地图继续使用
  注册点云，二维占据投影独占建图点云。固定包 A/B 中自由格变化 `-0.01%`，
  占据格由 4,053 增至 4,219（`+4.10%`），前端 P95 `25.345 ms`；在线探索
  12/12 个目标成功，自由格 `75,131`，四向墙外越界均为 0，0 碰撞、0 恢复、
  0 子图重初始化。
- [x] 提供版本化自包含快照的原子保存与恢复，以及 `mapping` 继续建图和
  `localization` 只读定位模式；恢复会重建 Scan Context、局部/全局匹配地图和
  二维投影。首版只定位要求从保存末端位姿启动，任意初始位姿全局重定位仍待实现。
  2026-08-11 补上 `mapping` 模式的端到端验收：`snapshot_resume_regression`
  在固定包上分两段跑"存档 → 重启 → 继续建图"，接缝峰值误差 `0.018 m` /
  `0.082°`，10/10 通过。此前该项只有单元覆盖。
  2026-08-11 格式升级为只读写 v3，保存两份关键帧点云并明确拒绝 v1/v2；v3
  保存并校验输入体素、高度带、射线范围和平面运动投影契约。删除二维栅格永不
  消费的高处回波后，393 帧快照由 `60.27 MB` 降至 `44.05 MB`（`-26.91%`），
  生产路径接缝峰值 `0.019 m / 0.062°`。
- [x] 随机化近优目标选择后重跑探索活动：10/10 `ACCEPTED`，0 次归因主机；原有门限
  最紧一项仍留 1.17 倍余量。真值行程离散度 `51%` 而最终自由格只有 `0.28%`，
  说明变的是路线不是覆盖；114 次选择中 100 次候选池为 1，随机化只在 12% 的
  岔口生效。该批含三处改动，属整体验收而非单变量归因。
- [x] 从 SDF collision 自动生成合法随机出生位姿：按外接圆与平面余量、整机高度与
  顶部余量检查三维扫掠体，只从参考原点所在的连通支撑面取样；同一非零种子可复现，
  批量样本至少相距 `2 m`。2026-08-12 在 `structured_loop_3d.sdf`、`slam_world.sdf`
  和 `large_warehouse.sdf` 各完成 5 次随机出生验收，合计 15/15 PASS。
- [ ] 补 `localization` 模式恢复的端到端回归。它与 `mapping` 共用同一份快照，
  但走只读全局点云匹配的另一条分支，目前无任何入口验证过。
- [ ] 在固定 rosbag 和结构化 3D 世界中与 RTAB-Map 对比漂移、闭环误差、
  地图重影、CPU、内存和端到端延迟。

当前 Gazebo 点云缺少逐点 `time`，因此仿真首版明确关闭 deskew，不伪造时间
字段。去畸变和完整 LIO 作为厂家数据集上的独立里程碑，不阻塞纯几何 3D
SLAM 与在线探索。

### 8.4 自研 3D 在线导航与 Frontier Exploration

该阶段不是重写 Nav2。自研 SLAM 负责实时定位、3D 地图和二维导航投影；
Nav2 继续负责全局规划、局部控制、避障和恢复；独立探索器从实时二维地图中
选择“已知自由空间—未知空间”边界，并通过标准 `NavigateToPose` 动作发送
目标。这与 Nav2 官方“边建图边导航”接口保持一致。

- [x] 建立自研 3D SLAM + Nav2 在线入口：不启动 Map Server/AMCL，自研 SLAM
  独占 `map -> odom`，Nav2 订阅持续扩展的二维投影地图。
- [x] 保持全局规划使用二维投影，局部代价地图直接使用 3D 点云高度带；既有
  高度语义回归已验证下细上粗障碍物绕行和高门洞通行。
- [ ] 补充 3D 在线探索期间的动态障碍物重规划和完全封路恢复回归。
- [x] 实现可维护的 ROS 2 frontier 探索器：聚类 frontier、过滤不可达
  候选，并按信息增益、路径代价和安全距离评分。
- [x] 处理地图更新和回环修正：当前目标失效、路径被占用或 frontier 消失时，
  取消旧目标并重新选择；不得让探索器与 Nav2 同时发布 `/cmd_vel`。
- [x] 增加探索完成判据：连续若干周期没有未拉黑的可达 frontier，或连续目标
  未带来最低自由区增长，且已知自由区达到最低规模后结束；自研 SLAM 通过服务
  保存最终版本化快照，RTAB-Map 继续使用数据库持久化。
- [x] 建立自动回归：未知结构化世界从固定出生点开始，无人工点击目标，检查
  已知自由区增长、导航成功/失败次数、真实行程、恢复次数、碰撞和关键日志。
- [x] 分别使用 RTAB-Map 和自研 3D SLAM 运行同一探索回归，确认探索逻辑与
  SLAM 实现解耦，并把成熟基线作为自研链路的最低验收参照。
- [x] 整改自研探索的概率栅格与地图交付：内部概率图与 Nav2 三态地图分离，
  新关键帧触发增量投影，三次自由射线后开放可通行格；2026-08-10 默认 900 s
  回归在 398.8 s 自行完成，得到 77,776 个自由单元、9/0 次导航成功/失败，
  并成功保存快照。
- [x] 为交互仿真补齐依赖失效边界：Gazebo 退出时联动关闭整套 launch；Nav2
  action server 运行中失活或拒绝 goal 时探索器进入 fault，不发布完成消息或
  保存快照。

## 9. 后续扩展

### 9.1 视觉

- 添加 RGB、双目或 RGB-D 相机。
- 配置相机内参、深度和图像桥接。
- 实现特征提取、匹配、视觉里程计和回环。

### 9.2 多传感器融合

- 统一相机、激光、IMU 和轮速时间戳。
- 标定传感器外参。
- 使用 EKF、因子图或紧耦合方法进行融合。

## 10. 开发与调试策略

每个阶段遵循以下流程：

1. 启动最少数量的节点。
2. 检查节点、话题和服务是否存在。
3. 检查话题频率、消息内容和时间戳。
4. 检查 TF 连通性和发布者。
5. 在 RViz 中检查数据空间对齐。
6. 最后再调整算法参数。
7. 记录启动命令、测试结果和已知问题。

问题排查优先级：

```text
仿真时间
-> TF
-> 传感器 frame_id
-> 里程计方向和尺度
-> 激光数据质量
-> SLAM 参数
-> 算法实现
```

## 11. 近期任务清单

- [x] 确认 ROS 2、Gazebo、SLAM Toolbox 和 Nav2 环境。
- [x] 编写项目协作规范。
- [x] 编写总体项目计划。
- [x] 创建 ROS 2 基础包。
- [x] 创建差速轮式机器人 Xacro。
- [x] 在 RViz 中验证机器人模型与 TF。
- [x] 在 Gazebo 中生成机器人。
- [x] 配置差速驱动和 ROS-Gazebo bridge。
- [x] 添加 2D 激光雷达。
- [x] 创建 SLAM 测试世界。
- [x] 接入 `slam_toolbox`。
- [x] 标定 Gazebo 差速驱动有效轮距，验证原地旋转时真值、里程计和 SLAM 航向对齐。
- [x] 验证占据栅格与 SLAM Toolbox 位姿图保存流程。
- [x] 在建图 launch 退出前自动保存占据栅格和 SLAM Toolbox 位姿图。
- [x] 优化 WSL2 图形后端、Gazebo 物理与渲染负载、RViz 刷新率和 SLAM 关键帧频率。
- [x] 完成第一张二维地图并保存。
- [x] 接入 AMCL 和 Nav2。
- [x] 配置真实机器人 footprint 并完成自动多目标导航回归。
- [x] 验证地图外临时障碍物的代价地图更新和动态绕障。
- [x] 增加 Gazebo 真值、rosbag 数据集和自研 C++ 激光预处理节点。
- [ ] 验证完全封路时的停车等待和 Nav2 恢复行为。
- [x] 开始自研 2D 激光 SLAM 模块。
- [x] 实现点到线 ICP 对照算法。
- [x] 实现基于轮式里程计预测的局部相关扫描匹配前端。
- [x] 对齐 10 Hz 激光与 50 Hz 里程计 TF，并消除 RViz 点云间歇性时间同步错误。
- [x] 实现自研占据栅格地图发布。
- [x] 实现自研 `map -> odom` 校正并验证原始点云与匹配点云对齐。
- [x] 建立 Ceres 二维位姿图后端、顺序约束和回环约束求解测试。
- [x] 实现历史候选筛选、激光回环验证和自动位姿图优化。
- [x] 按优化后的全部关键帧分批重建自研占据栅格。
- [x] 增加自研地图保存、长闭环和快速旋转回归。
- [x] 增加单向退化走廊回归，验证横向/航向漂移和错误回环保护。
- [x] 增加走廊返回路线回归，验证起点闭合、真实回环和强/弱观测切换。
- [x] 增加重复结构混淆回归，验证相似房间不误闭环且真正重访可闭环。
- [x] 增加大场景长时间仿真回归，验证 2400+ 节点、多次回环重建和资源上限。
- [x] 增加固定 rosbag 的 2× 长时间离线回放回归，并记录数据集指纹。
- [x] 冻结 2D SLAM 基线并完成进入 3D 前的文档一致性检查。
- [x] 增加参数化、互斥的 3D LiDAR 模型、独立 TF、点云桥接和 RViz 显示。
- [x] 建立 3D LiDAR 频率、时间戳、点云字段、空间对齐和基础性能验收。
- [x] 增加参数化 IMU、默认二维 EKF 和正常/打滑对比回归；历史算法回归
  显式固定纯轮式输入。
- [x] 接入 MOLA 官方 GICP 纯激光里程计，增加点云契约检查并完成首次运动验收。
- [x] 接入 RTAB-Map 在线 3D LiDAR SLAM，增加独立参数、地图 RViz、SQLite
  持久化和不影响现有 TF 的启动入口。
- [x] 接入 RTAB-Map + Nav2 在线导航入口，并把二维栅格契约检查接到实际
  发布的 `/rtabmap/map` 上验证。
- [x] 录制固定 3D rosbag，建立输入契约检查和回放入口；自研前端完成后补充
  与 RTAB-Map 的同输入误差判定器。
- [x] 实现自研 3D GICP scan-to-local-map 首版、有界关键帧局部子图和弱方向
  连续退化处理；三档快速旋转与单侧打滑回归通过，地面分类与离群点过滤
  继续作为前端预处理收尾任务。
- [x] 按“全局关键帧库 → Scan Context 候选检索 → GICP 几何复核 → 后台 SE(2)
  位姿图 → 全局地图重建与二维投影”的顺序实现自研 3D 后端。
- [x] 接入自研 3D SLAM + Nav2 在线建图导航，不启动 Map Server/AMCL。
- [x] 接入 Frontier Exploration，并完成未知世界自主探索与自动保存回归。
- [x] 修正 3D 探索二维投影的地面自由空间语义、增量栅格更新、动作看门狗和
  同场景覆盖门槛；固定世界回归要求至少 38,000 自由单元和 35 m 真值行程。
- [x] 完成探索稳定性整改：Scan Context 改用无量纲余弦距离并限制回环密度，
  frontier 排名避免双重惩罚远目标，失效目标进入空间黑名单；回归增加地图宽高、
  包围盒、目标数量、子图重启与检索漏斗诊断。

2026-08-04 在线 3D SLAM 审查整改：

- [x] 测量输入同步率：临时解除 `Rtabmap/DetectionRate` 限速后对比
  `/rtabmap/info` 与点云频率，确认精确时间同步在本仿真中未丢帧，但成立
  原因是两个时间戳栅格恰成整数倍；改用 `approx_sync` 并按里程计频率
  推导 `approx_sync_max_interval`，使其不依赖该巧合。
- [x] 把点云与里程计的订阅 QoS 分开写明：点云必须 best effort 以匹配
  `ros_gz_bridge`，里程计改为可靠订阅，并在配置中注明原因。
- [x] 显式写出 `Rtabmap/DetectionRate`，使性能文档引用的频率是配置选择
  而非继承的默认值。
- [x] 对齐导航高度带：按含 3D 雷达的 `0.35 m` 机器人高度加 `0.10 m`
  余量，统一 RTAB-Map、局部体素层、全局障碍层和 Collision Monitor 的
  `0.45 m` 上限；局部体素层以 `15 × 0.03 m` 精确覆盖该高度。
- [x] 使 MPPI `CostCritic` 与两个代价地图使用同一个八边形 footprint，消除
  圆形碰撞近似对本车非对称长尾外形的漏检风险。
- [x] 增加两圈结构化 3D 自动回归；首次固化运行 `143.1 m`、`346` 个图节点、
  `181` 次 proximity 约束，航迹推算 `0.531 m` 漂移经 SLAM 收敛到
  `0.004 m / 0.075°`，全部硬判据通过。
- [x] RViz 占据栅格显示改为 Transient Local 订阅，匹配 RTAB-Map 的
  latched 发布，避免静止时重启 RViz 后地图空白。
- [x] 复查「叠加 Nav2 后 EKF 周期超时」：原结论不成立。超时来自测量环境中
  遗留的多个仿真实例（`kill -INT` 未回收 launch 子进程树），隔离运行后
  完整导航链路在 `137 s` 内只有一次 `24 ms` 周期，裕度充足。
- [ ] 排查两个代价地图的实际发布率低于配置值（全局 `0.500` vs `1.0`，
  局部 `1.667` vs `2.0`），确认是 `update_frequency` 约束、仿真时间换算
  还是别的原因。
- [x] 高度语义导航回归结束后检查无残留 ROS/Gazebo 进程，避免时序指标被
  上一次运行的泄漏实例污染。

2026-07-30 工程审查整改：

- [x] 自研 SLAM 默认使用 `RelWithDebInfo`，确认实时目标启用 `-O2`。
- [x] 补齐 LaserScan、里程计、TF 和匹配输入的有限性检查。
- [x] 增加节点构造、扫描回调和地图定时器异常边界。
- [x] 将基础二维位姿测试从 ICP 测试中拆分，并修复大角度归一化。
- [x] 将关键帧/位姿图写入改为事务式，并在回环优化失败时保留原始图。
- [x] 将回环检测与位姿图优化迁移到只处理状态快照的后台工作线程。
- [x] 修复地图重建重启饥饿，使用仿真时钟驱动重建和地图发布。
- [x] 对 Path 按移动阈值降采样、限制长度并降至 2 Hz 发布。
- [x] 用累计行程替代纯关键帧数作为主要回环历史门限。
- [x] 增加连续候选链、最大修正量和扫描支撑占比回环判据。
- [x] 匹配分数只对局部栅格有效范围内的点归一化，并单独限制支撑率。
- [x] 在激光时间戳处插值轮式里程计，并补齐输出协方差。
- [x] 修正 720 束 360° LiDAR 的首尾重复方向。
- [x] 解耦相关匹配粗搜索步长与精搜索窗口，减少实时候选数量。
- [x] 将 `Point2D` 移到纯几何头文件，解除几何库对 `sensor_msgs` 的传递依赖。
- [x] 补齐库目标导出、维护者信息、CMake 最低版本和自动 lint。
- [x] 导航回归使用仿真时间判定场景超时，并为 Nav2 激活增加墙钟上限。
- [x] 将占据栅格从逐单元哈希改为 16×16 分块稠密存储。
- [x] 抽出并测试“完成当前版本、只排队最新版本”的地图重建状态机。
- [x] Path 达到上限后按批次裁剪，避免每新增一个位姿都整体搬移。
- [x] 建立逐条工程审查整改记录，标明运行链路修复和有意保留项。
- [x] 增加两圈长闭环自动回归，验证后台回环、地图重建、前端间隔和地图保存。
- [x] 收口地图重建和回环提交后的异常边界，合并候选评分重复栅格查表。
- [x] 将回环候选链提高到 10、旋转修正上限收紧到 0.25 rad，并复跑闭环。
- [x] 增加三档快速旋转自动回归，验证闭合误差、前端连续性、地图稳定性和误回环。
- [x] 建立退化长走廊专用世界和单向自动回归，记录不可观测纵向漂移基线。
- [x] 根据局部相关响应识别平移弱观测方向，沿该方向保留里程计预测并发布各向异性协方差。
- [x] 复跑长走廊、三档快速旋转和两圈长闭环，验证退化处理与正常场景兼容。
- [x] 增加逐次匹配诊断，区分运动不足、接受和拒配并输出可观测性统计。
- [x] 用真实 720 束角采样、不同子图深度和测距噪声建立走廊特征测试矩阵。
- [x] 用保留光束索引和表面断裂检测的扫描法向几何量替换响应 Hessian 判据。
- [x] 按法向信息比值连续缩放弱方向修正，消除临界区位姿硬切换。
- [x] 增加正常场景零误报回归；长闭环和三档快速旋转分别要求真实匹配
  存在、诊断可解析且 `degenerate_events == 0`。
- [x] 将单帧法向可观测性带入后台回环，保守拒绝 rank 小于 2 的候选，
  并复跑两圈长闭环验证正常回环不受损。
- [x] 将位姿图平移标量权重升级为对称 3×3 信息矩阵，并测试正定性、
  Ceres 白化和弱方向低权重回环。
- [x] 抽出输出位姿协方差模型；成功匹配重置观测协方差，航位推算按相邻
  里程计行程和转角累计过程噪声。
- [x] 走廊回归按物理中段统计逐次退化检出比例，并同时设置样本数下限。
- [x] 增加轮速/IMU 薄适配节点，为 Gazebo 裸消息补充有限、非零且可调的
  各向异性协方差。
- [x] 将 IMU 融合测量工具升级为自动回归：正常路面限制劣化，打滑路面
  要求航向改善，并检查两路协方差契约。
